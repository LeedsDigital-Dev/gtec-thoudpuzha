# workflow
- When populating seed/demo data for visual review, fills every available field with realistic, comprehensive content — not minimal stubs — so the UI looks complete and can be properly inspected. The goal is production-quality demo data, not just enough to make tests pass. Confidence: 0.85
- Before running a data mutation script (enrichment, migration, seeding), verifies the actual database state matches the script's assumptions — e.g., checking actual slugs/IDs against the script's lookup keys — rather than blindly running and hoping they align. Confidence: 0.75
- For non-trivial one-off data operations (seeding, enrichment, migration), prefers writing a standalone TypeScript script in `scripts/` run with `npx tsx` — with proper imports, error handling, and a `main()` function — rather than inline `node -e` one-liners or raw SQL. Confidence: 0.65
- For long-running shell commands (npm install, test suites, builds): start in background and poll with a short command instead of waiting directly and hitting the tool-call timeout. Confidence: 0.75
- Treat tool-call timeouts on long commands as expected behavior to route around with background+poll, not as bugs to be solved. Confidence: 0.70
- When resolving divergence on main/master branch: always sync local to match remote exactly and discard all local changes (`git reset --hard origin/main && git clean -fd`). Favors remote over local. Confidence: 0.70
- Before building new functionality, search for an existing implementation first. If one exists but isn't wired up, just connect it rather than rebuilding. Only build from scratch when nothing reusable exists. Confidence: 0.85
- When extending a feature (e.g., adding a new role to a signup flow), follow the existing patterns already established for similar cases in the codebase — don't invent a new approach. Confidence: 0.75
- When adding or changing i18n translation keys, update ALL locale files in parallel (en.json, ml.json) — never leave translations out of sync. Confidence: 0.80
- Before using a third-party component or API named in a spec, verify it actually exists in the installed version by checking node_modules type definitions — don't trust specs at face value about API surface. Confidence: 0.85
- When new library imports break existing tests, add a global mock in the test setup file (setup.ts) rather than wrapping individual test files with providers. Confidence: 0.80
- When an audit uncovers out-of-scope issues, flag them explicitly in the completion report rather than silently fixing them or staying quiet. Confidence: 0.75
- Prefers planning before coding: enters plan mode, explores the codebase thoroughly across multiple dimensions, writes a detailed plan (with rationale, file lists, and verification steps) to ~/.commandcode/plans/, presents it for approval, then implements. Confidence: 0.85
- Prefers parallel exploration: when researching a codebase before implementing, launches multiple independent exploration agents simultaneously to investigate different aspects (auth, layouts, components, i18n) rather than exploring sequentially. Confidence: 0.75
- When media assets are missing from storage (R2/S3/CDN), prefers a silent graceful fallback (e.g., 1×1 transparent PNG) over returning a 404 error that clutters the console. Confidence: 0.65
- When migrating a file to a new convention/location that may still have existing importers, keeps a thin backward-compatible re-export shim with a TODO comment to remove it later. Confidence: 0.70
- Prefers conventional commits format (`type: summary`) with a descriptive body explaining the "why" of the change, not just the "what". Confidence: 0.85
- When asked for a commit message, inspects actual git state first (`git status --short`, `git diff --stat`, `git log --oneline`) before composing the message rather than drafting from memory. Confidence: 0.70
- When providing commit messages, offers both a detailed full version (with structured breakdown by area — what was done, infrastructure, bugs found/fixed) and a compact short version, letting the user pick. Confidence: 0.70
- Prefers staging specific files (`git add <file1> <file2>`) over bulk staging (`git add .` or `git add -A`). Confidence: 0.70
- For multi-step tasks, uses structured todo lists (todo_write) to plan, track progress, and mark phases complete — not ad-hoc sequencing. Confidence: 0.80
- Verifies build succeeds (`npm run build`) before committing deployment-readiness or infrastructure changes, not just after. Confidence: 0.70
- Avoids importing devDependencies (e.g., dotenv) in files that run in production builds; relies on the platform's native env-var injection instead. Confidence: 0.70
- When a deployment platform provides its own dynamic URL env var (e.g., VERCEL_URL), prefers it over a custom NEXT_PUBLIC_* variable for resolving the canonical app URL at runtime. Confidence: 0.65
- Prefers JSON/JSONB columns (via Prisma `Json` type) for storing heterogeneous, user-editable content structures where different entities may have different sets of fields — the UI adapts to whatever data is present rather than enforcing a rigid schema. Confidence: 0.75
- Demands runtime validation (Zod) on all user-submitted data, not just TypeScript compile-time types — "proper typechecks" means Zod schemas with min/max length, required fields, and discriminated unions. Confidence: 0.80
- Prefers a "preview before publish" pattern for admin-edited content: the admin sees a live preview tab rendering the same public-facing component with in-memory state before saving/publishing to the database. Confidence: 0.70
- Writes unit tests for validation schemas (valid, invalid, edge cases, boundary lengths) and server actions (success, auth rejection, malformed input, audit logging) for every new feature — not just integration/E2E tests. Confidence: 0.75
- Prefers content-blocks architecture for pages with heterogeneous sections: each section (hero, course lists, benefits) is a typed block in a JSON array, and the rendering component iterates them, only rendering sections with non-empty data. Confidence: 0.70
- Prefers idempotent seed scripts: uses `upsert` or `findFirst` + `update`/`create` so the seed is safe to run multiple times without duplicating data or throwing constraint errors — never delete-then-recreate. Confidence: 0.80
- Adds `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer to every commit. Confidence: 0.70
- When reporting `git pull` results, provides a summary including the commit range, merge type (fast-forward vs merge), and a brief classification of what changed — not just a terse "Already up to date." Confidence: 0.60
- Expects deployment plans to include a complete, structured listing of every environment variable and secret key — with the variable name, whether it's required, where to obtain the value, and any platform-specific notes — not just a narrative summary. Confidence: 0.70
- When debugging a database connection issue, tests at progressively higher abstraction layers — raw CLI client (`psql`) first, then the native driver library (`pg`), then the ORM (`Prisma`) — to isolate whether the problem is at the protocol, driver, or ORM level. Confidence: 0.75
- Keeps `.env` for production/infrastructure config and `.env.local` for local development overrides, respecting Next.js's env-file layering so that local dev doesn't accidentally point at production resources. Confidence: 0.70
- After migrating data between databases, verifies row counts for every affected table — not just spot-checks a few — to confirm the migration is complete and consistent. Confidence: 0.75
- When a code edit is recognized as incorrect, reverts it immediately rather than leaving it and fixing later — won't let a known-bad change sit in the file. Confidence: 0.70
- After migrating away from a dependency across multiple files, performs a final codebase-wide grep sweep (matching the old package name/import path across all source files) to confirm zero remaining references before considering the migration complete. Confidence: 0.75
- After npm install/uninstall operations, explicitly verifies the final dependency state in package.json (new packages present in the correct dep category, old packages absent) rather than trusting the npm CLI output alone. Confidence: 0.70
- When a library produces a confusing error (e.g., mangled URL, nonsensical hostname), investigates the library's internal implementation in node_modules (grep through minified dist files, probe exports with node -e) to understand root cause before deciding on a fix — doesn't treat dependencies as black boxes. Confidence: 0.70
- When a fork remote exists alongside origin, pushes commits to both remotes (not just origin) — user expects changes synced to both. Confidence: 0.75
- Prefers live, real-time debugging via platform-native CLI tools (e.g., Netlify CLI) over post-hoc log analysis from a dashboard — install the CLI, deploy, and watch logs as they stream to pinpoint errors immediately. Confidence: 0.75
- When placing inline `<script>` tags that manipulate the DOM (e.g., calling `document.body.appendChild`), places them inside the `<body>` element rather than in `<head>`, since `document.body` is null during `<head>` execution. Confidence: 0.80
- When Lighthouse or other performance measurement tools show unexpected score degradation after changes, explicitly checks for localhost-only artifacts (auth provider dev-mode redirects, dev instance key mismatches, network filesystem overhead) before concluding the application changes caused the regression — these don't reflect production behavior. Confidence: 0.70
- For web performance auditing, uses Lighthouse CLI installed as a devDependency (`npm install --save-dev lighthouse`), runs against the local dev server, tests both desktop (`--preset=desktop`) and mobile (`--preset=perf`) configurations, and tests multiple page types (homepage, detail pages) — not just a single page. Saves HTML reports to a `reports/` directory. Confidence: 0.70
- Extracts Lighthouse scores programmatically by parsing the embedded `__LIGHTHOUSE_JSON__` from the HTML report using `node -e`, rather than relying solely on opening the visual report in a browser. Confidence: 0.65
- When the Next.js dev server is too slow to compile for Lighthouse testing (common on network filesystems or resource-constrained environments), prefers running `next start` against the production build instead — it's both faster to serve and gives more realistic scores that reflect what users actually experience. Confidence: 0.70
- Wraps data-fetching functions in `React.cache()` so Next.js deduplicates calls between `generateMetadata` and the page render within the same request — avoids duplicate DB queries when both the metadata generator and the page component fetch the same record. Confidence: 0.75
- When encountering N+1 query patterns (sequential per-entity DB calls in a loop or a series of individual checks), creates a single batch query helper that fetches all data in one round-trip, and uses role-based bypass (e.g., Super Admin skips permission checks entirely) to short-circuit unnecessary DB calls for privileged users. Confidence: 0.70
- When a page uses ISR (`revalidate`), also provides `generateStaticParams` so pages are pre-rendered at build time — the first visit is instant from the CDN edge, then ISR keeps content fresh. Confidence: 0.70
- When fetching "related items" for a sidebar or card grid (e.g., related courses, similar products), creates a lightweight dedicated query with only the needed fields and a tight row limit — never reuses the full list query that fetches all rows with all columns. Confidence: 0.65
- After completing a multi-step implementation, produces a structured completion summary with concrete metrics (files modified, DB queries saved, pages pre-built, etc.) in a compact table format — not just a narrative recap. Confidence: 0.65
- When a third-party library integration proves fundamentally incompatible with the framework version (type conflicts, API mismatches requiring escalating adapter layers), recognizes sunk cost early and pivots to a simpler manual implementation rather than persisting through increasingly fragile workarounds — the plan is a starting point, not a contract. Confidence: 0.80
- When running E2E test suites, prefers incremental flow-by-flow verification: run one role's flow to completion, fix all issues until it's clean, then move to the next — not running everything in parallel and sorting through a pile of failures afterward. Confidence: 0.90
- When a Playwright locator fails (element not found, strict mode violation, wrong match), reads the actual source component file to understand the real rendered DOM structure before adjusting the locator — doesn't guess from the error context alone. Confidence: 0.80
- When Clerk email/password sign-in isn't viable for E2E tests (e.g., JWT claims not configured, redirect loops), switches to Clerk Agent Tasks (`clerk.agentTasks.create({ onBehalfOf: { userId }, redirectUrl })`) for programmatic authenticated sessions rather than fighting the sign-in UI. Confidence: 0.85
- For Playwright global setup files that call library functions (e.g., `clerkSetup()`), the file should export a plain `async function` as default — NOT use `test.describe.configure()` or import `test` from Playwright, since global setup runs as a config-level script, not a test. Confidence: 0.75
- When `getByLabel("Password")` causes strict mode violations in Clerk sign-in forms (the hidden "Show password" button also matches), uses `getByPlaceholder("Enter your password")` as an alternative. Confidence: 0.70
- When seeding test data for E2E flows, creates real auth provider users (not just DB stubs) via the provider's API, then creates DB records linked to those real user IDs — so tests can authenticate with actual credentials through the full auth stack rather than mocking or bypassing auth. Confidence: 0.80
- Creates granular npm scripts for running individual test flow files by role/area (`test:e2e:visitor`, `test:e2e:student`, `test:e2e:employer`), not just a monolithic `test:e2e` command, enabling isolated debugging and faster iteration on specific flows. Confidence: 0.75
- When asked to build something complex or novel, first assesses feasibility before committing to implementation — researches blockers (auth, third-party APIs, infrastructure), explores existing code/test infrastructure, consults external docs (web searches, library docs), and gives a definitive yes/no with rationale before any planning begins. This is a pre-plan gate, distinct from the planning phase itself. Confidence: 0.80
- For multi-step, stateful user flows (signup, checkout, multi-page workflows), prefers organizing E2E tests in per-flow subdirectories under `tests/e2e/flows/`, using `test.describe.serial()` for sequential execution where each test is a step that depends on state accumulated by prior steps. Confidence: 0.80
- When writing end-to-end tests for a flow, expects tests to be diagnostic tools, not just verification checklists — when a test breaks, the follow-through is to trace the root cause and fix the underlying bug in the application code, not just report the failure and move on. Confidence: 0.90
- For portal/authenticated sections on mobile, prefers a fixed bottom navbar (icon+label tabs with a Clerk UserButton avatar for account/sign-out) over a sidebar with hamburger/offcanvas trigger. Desktop keeps the sidebar; bottom nav is `md:hidden`. Confidence: 0.85
- When swapping between two UI patterns for mobile vs desktop (e.g., sidebar ↔ bottom nav) — especially with shadcn/ui components that have built-in responsive behavior like Sidebar auto-rendering as an offcanvas Sheet via useIsMobile() — uses conditional rendering (`{!isMobile && <DesktopOnly />}` / `{isMobile && <MobileOnly />}`) so the unwanted component is completely absent from the DOM, not just CSS-hidden or trigger-removed. Confidence: 0.80
- When asked for regression testing documentation, produces a comprehensive, role-by-role checklist in checkbox-markdown format that exhaustively maps every workflow — explores the actual codebase (routes, auth config, page files) to build the checklist rather than guessing from memory. Saves the artifact to `docs/`. Confidence: 0.85
- When a requirement has multiple valid interpretations (e.g., mobile-only vs all-screen, UserButton dropdown vs explicit logout button), asks the user structured clarifying questions with labeled options and descriptions before committing to an implementation approach — doesn't guess and plow ahead. Confidence: 0.75
 across many files (e.g., responsive padding, table overflow wrappers), batch-greps for the pattern first to discover all occurrences, then systematically fixes each one — doesn't fix files one at a time without knowing the full scope. Confidence: 0.80
- Prefers live, real-time debugging via platform-native CLI tools (e.g., Netlify CLI) over post-hoc log analysis from a dashboard — install the CLI, deploy, and watch logs as they stream to pinpoint errors immediately. Confidence: 0.75
