[spawn-engineer] s03-t5  tier=cheap  profile=deepseek-flash  model=deepseek/deepseek-v4-flash
=== Attempt 1/3 (2026-07-23T04:27:27Z) ===
**Finished successfully.** All 104 tests pass across 25 files on `main`. Here's what was delivered:

**Schema**: Added `slug` (unique) to `GalleryCategory` via migration `20260723100000_add_gallery_category_slug` — backfills existing rows + handles duplicates.

**Admin gallery actions**: `createCategory` auto-generates a unique slug from `nameEn` using `slugFromName()`.

**lib/gallery.ts**: Added `getGalleryCategoryBySlug(slug, limit?)`, `getPlacementGalleryData()`, and `slugFromName()` helper.

**Seed**: `prisma/seed.ts` creates a "Placement & Support" category with slug `placement-support` — run via `npm run db:seed`.

**PlacementSupportSection**: Data-as-props component (same pattern as all other homepage sections). Shows a media grid (up to 6 items) from the Placement category, "View full gallery" deep-link to `/gallery?category=placement-support`, and a CTA banner with links to `/portal/jobs` and `/portal/employer/register`.

**Gallery page**: Accepts `?category=` search param; `GalleryGrid` accepts `initialCategorySlug` prop to pre-select the matching tab.

**Homepage**: Section wired between `WhyChooseUsSection` and `ContactSection`. Returns null when no data exists (no blank space).

**4 tests pass**: correct category rendering, item count limiting (via bound in the lib query), gallery deep-link, and CTA routes.
--- exit code: 0 ---
[spawn-engineer] s03-t5 VERIFIED done (commit landed, status flipped).
